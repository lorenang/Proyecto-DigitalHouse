<!DOCTYPE html>
<html lang="en">
    <%- include('../head.ejs') %>
<body>
    <%- include('../header.ejs') %>
    <main>
        <section>
            <h2 class="center" style="color: red;"> Estimado Cliente: <%= user.firstName %> <%= user.lastName %> </h2>
            <% if(locals.user && user.categoria === "user") {%>
                <h2 class="center" style="color: red;"> ¿Está seguro/a de eliminar esta cuenta? </h2>
            <% } else {%>
                <h2 class="center" style="color: red;"> ¿Está seguro/a de eliminar la cuenta de: <%= userToDelete.firstName %> <%= userToDelete.lastName %>? </h2>
            <% } %>
                
            <br>
            <br>
            <% if(locals.user && user.categoria === "cliente") {%>
                <div id="form-buttons"> 
                    <form action="/user/delete/<%= user.id %>?_method=DELETE" method="POST">
                        <button type="submit" class="">ELIMINAR CUENTA</button>
                    </form>
                    <a href="/user/profile" class=""><button type="button" class="">CANCELAR</button></a>
                </div>  
            <% } else { %>    
                <div id="form-buttons"> 
                    <form action="/user/delete/<%= userToDelete.id %>?_method=DELETE" method="POST">
                        <button type="submit" class="">ELIMINAR CUENTA</button>
                    </form> 
                    <a href="/user/usersList" class=""><button type="button" class="">CANCELAR</button></a>
                </div> 
            <% } %>   
                   
        </section>

        <p></p>
        <p></p>
        <p></p>
    </main>
    
    <%- include('../footer.ejs') %>
</body>
</html>